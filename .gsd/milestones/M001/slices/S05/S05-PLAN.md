# S05: CLI Entry Points

## Goal
Create CLI commands for vibemate.

## Tasks

### T024: Create `npx vibemate-mcp` entry
MCP server entry point.

### T025: Create `npx vibemate install` entry
Config injector entry point.

### T026: Create `npx vibemate spec` entry
Direct skill call for testing.

### T027: Add help/version flags
CLI argument parsing.

### T028: Unit tests for CLI parsing
Test all CLI commands.

## Output
- `src/cli/index.ts` - CLI entry point
- `tests/cli/index.test.ts` - Unit tests
