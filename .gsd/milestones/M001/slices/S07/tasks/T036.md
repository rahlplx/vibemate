# T036: Auto-Complete — MCP Tool Registration

## Description
Register auto-complete as an MCP tool named `vibemate_suggest` that returns suggestions for spec generation.

## Acceptance Criteria
- Tool registered as `vibemate_suggest`
- Schema: `{ query: string; context?: { idea?: string; stack?: string } }`
- Returns structured suggestions as tool result
- Integrates with request/response logging

## Dependencies
- T034, T035

## Test File
`tests/mcp/auto-complete-tool.test.ts`
