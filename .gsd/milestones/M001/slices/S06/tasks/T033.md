# T033: Auth Error Handling — Expiration, Re-auth, Rate Limits

## Description
Implement auth error handling: expired token detection, re-auth guidance, rate limit handling.

## Acceptance Criteria
- Detects expired token and returns re-auth guidance message
- Rate limit exceeded returns actionable error with retry-after
- Auth errors include human-readable messages
- Integration: installer detects expired token during config injection

## Dependencies
- T029, T030, T031

## Test File
`tests/mcp/auth-errors.test.ts`
